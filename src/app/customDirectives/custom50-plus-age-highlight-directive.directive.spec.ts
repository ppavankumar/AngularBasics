import { Custom50PlusAgeHighlightDirectiveDirective } from './custom50-plus-age-highlight-directive.directive';

describe('Custom50PlusAgeHighlightDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Custom50PlusAgeHighlightDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
